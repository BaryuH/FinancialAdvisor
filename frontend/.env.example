# Copy to .env.local for local overrides. On Vercel, set this in Project → Settings → Environment Variables.
# Must include the /api/v1 path (see src/app/lib/api/client.ts).
VITE_API_BASE_URL=https://your-api-host.example.com/api/v1
